# Initiative Stub Checklist Template

**Status:** PASS 2 CHECKLIST  
**Initiative:** `<initiative-name>`  
**Domain:** `<domain>`  
**Last Updated:** `<YYYY-MM-DD>`

## Types Created

1. `<type-name>` - `<purpose>`
2. `<type-name>` - `<purpose>`

## Interfaces Created

1. `<interface-name>` - `<contract>`
2. `<interface-name>` - `<contract>`

## Wiring Points Updated

1. `<entrypoint/service/path>`
2. `<entrypoint/service/path>`
3. `<entrypoint/service/path>`

## Legacy Paths Removed or Disabled

1. `<legacy-path>` - `removed` | `disabled`
2. `<legacy-path>` - `removed` | `disabled`

## Compile-Safe Stub Behavior Checks

1. `<behavior-check-1>`
2. `<behavior-check-2>`
3. `<behavior-check-3>`

## Test Skeleton Coverage Map

| Case ID | Skeleton Test File | Test Name | Notes |
|---|---|---|---|
| C01 | `<path>` | `<name>` | `<notes>` |
| C02 | `<path>` | `<name>` | `<notes>` |
| C03 | `<path>` | `<name>` | `<notes>` |

## Gate Output Requirement

1. End Pass 2 report with an explicit request for Pass 3 approval.
2. If approval not provided, state: `blocked waiting approval`.

